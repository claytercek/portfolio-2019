<header>
	<div class="c-header--bar">
		<a class="c-header--title" href="{{site.link}}">Clay Tercek</a>
		<button class="c-header--hamburger js-wave-toggleMenu">
			<div class="c-header--hamburgerWrapper">
				<span></span>
				<span></span>
				<span></span>
				<span></span>
			</div>
		</button>
	</div>
	<canvas class="c-menuCanvas"></canvas>
	<div class="c-header--behind">
		<nav>
			<ul class="c-nav-main">
				{% set i = 1 %}
				{% for item in menuPrimary.items %}
					<li class="c-nav-main--item {{ item.classes|join(' ') }}">

						<a class="c-nav-main--link" target="{{ item.target }}" href="{{ item.link }}">
							<h4 class="c-nav-main--num u-font-beta">{{i}}</h4>
							<h2 class="u-font-delta">{{ item.title }}</h2>
							
						</a>
					</li>
				{% set i = i + 1 %}
				{% endfor %}
			</ul>
			<ul class="c-nav-secondary">
				{% for item in menuSecondary.items %}
					<li class="c-nav-secondary--item {{ item.classes|join(' ') }}">

						<a class="c-nav-secondary--link" target="{{ item.target }}" href="{{ item.link }}">
							<h2 class="u-font-delta">{{ item.title }}</h2>
							
						</a>
					</li>
				{% endfor %}
			</ul>
		</nav>
	</div>
</header>