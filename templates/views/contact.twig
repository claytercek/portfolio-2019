{% extends "baseWave.twig" %}
{% block content %}
	<main class="c-about  u-font-beta l-headerPadding">
		<div class="text u-spacing--half">
				{% include 'contactForm.twig' %}
		</div>
		<ul class="social">
			{% for item in menuSocial.items %}
			<li><a target="_blank"  href="{{item.url}}">{{item.title}}</a></li>
			{% endfor %}
		</ul>
	</main>
{% endblock %}